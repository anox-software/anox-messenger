# S1 — PROVENANCE_VERIFIED_NATIVE_RUNTIME Evidence

REMEDIATION-SESSION-S1-BUILD-PROVENANCE-001
MSC-UNIT-001 / MSC-UNIT-002 (FCP-1 runtime evidence record)

This document records the runtime evidence produced during the S1 session.
It is an evidence *record*, not an independent retest: INDEPENDENTLY_RETESTED
remains PENDING for every unit.

## Authoritative build lineage

- Build driver: `tools/security/native_build.py`
- Toolchain pin: `crypto/rust/rust-toolchain.toml` (channel 1.97.1, targets
  `aarch64-linux-android`, `x86_64-linux-android`)
- NDK: r26c (26.2.11394342); cargo-ndk 4.1.2
- Source SHA bound in manifest: `0f932520393feee6d479cc099f179f5766323125`
- Manifest: `build/native/native-manifest.json` (generated; not committed —
  regenerated by `native_build.py build`)

## BUILD_ARTIFACT_HASH_PROOF

Two clean builds produced identical per-ABI SHA-256 (`native_build.py
rebuild-compare` → PASS).

| ABI       | SHA-256 |
|-----------|---------|
| arm64-v8a | c854de2587ec1bd6387f39770feeabf24bf6cf2b729911c5ba57f5e4d8bd7b57 |
| x86_64    | 77a404b2bd6f13ce50b4360f439bd09e6cff6f6d8233557fe89c195fa33d85e2 |

`native_build.py verify` → PASS (artifacts match manifest; ABI set + JNI
parity verified — 17 JNI exports per artifact, matching the 17 Kotlin
`external` declarations in `crypto/android/.../CryptoNative.kt`).

## Packaged-artifact binding (AC-012 breaker)

`tools/security/validate_apk_contents.py` (v2) bound the packaged `.so`
bytes inside the built APKs to the manifest hashes:

- `android-debug.apk` → PASS (arm64-v8a + x86_64 hash-bound)
- `android-release-unsigned.apk` → PASS (arm64-v8a + x86_64 hash-bound)

## PROVENANCE_VERIFIED_NATIVE_RUNTIME — arm64-v8a

`./gradlew :android:connectedDebugAndroidTest` on local emulator
`anox_api34_arm64` (API 34, `ro.product.cpu.abi = arm64-v8a`):

- 66 instrumented tests executed, **0 failures, 0 errors, 0 skipped**
- Coverage included `com.anox.crypto.CryptoInstrumentedTest` (JNI surface,
  incl. BufferTooSmall / fixed-buffer behavior), DeviceAuth keystore tests
  (`AndroidKeystoreDeviceAuthKeyManagerTest`), binding-store and
  registration-session storage tests.
- The tested APK was assembled from `$rootDir/build/native/jniLibs` (the
  manifest-bound artifacts) and re-validated against
  `native-manifest.json` — the same artifact lineage that was hashed.

Result XML: `android/build/outputs/androidTest-results/connected/debug/`
(test-result artifacts are build outputs, not committed).

## x86_64

`RUNTIME_ENVIRONMENT_UNAVAILABLE` locally — no x86_64 emulator/system image
on this host. The executable path exists in CI:
`.github/workflows/ci.yml` job `instrumented-x86_64` (KVM + Android
emulator, `connectedDebugAndroidTest`, APK re-bound to the manifest before
and after the run). Independent Build/Supply retest must produce the
x86_64 runtime evidence.

## Status (as recorded by the isolated S1 session — historical)

- MSC-001: IMPLEMENTED + AUTOMATED_TESTED + RUNTIME_TESTED (arm64,
  provenance-bound) — INDEPENDENTLY_RETESTED / ATTACKCHAIN_RETESTED pending.
- MSC-002: IMPLEMENTED + AUTOMATED_TESTED + RUNTIME_TESTED (arm64) —
  INDEPENDENTLY_RETESTED pending.
- No unit is CLOSED. Independent Build/Supply retest required.

## Integration addendum — REMEDIATION-S1-CANONICAL-INTEGRATION-001

The text above is preserved verbatim as the isolated S1 record. Under the
exact FCP-1 semantics enforced since the canonical integration (retest
finding F3), the recorded arm64-only, implementer-run evidence does **not**
constitute `RUNTIME_TESTED = PASS`: FCP-1 requires same-run
`BUILD_ARTIFACT_HASH_PROOF + PROVENANCE_VERIFIED_NATIVE_RUNTIME` for **every**
required ABI (`arm64-v8a` and `x86_64`). `msc_state.jsonl` therefore carries
`RUNTIME_TESTED = PENDING` for MSC-UNIT-001/002 with the arm64 run retained as
`partial_evidence` (independent = NO). The independent retest
(`INDEPENDENT-BUILD-SUPPLY-RETEST-S1-001`) reproduced the two per-ABI artifact
hashes above byte-identically from clean builds but could not execute the
instrumented suite (no JRE / Android SDK / emulator on the retest host);
x86_64 runtime evidence remains `PENDING_REAL_CI_OR_INDEPENDENT_RUNTIME_EVIDENCE`.
