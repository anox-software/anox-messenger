# CURRENT_NEXT_DEVIN_TASK — anoX V1

**Event:** `ANOX-EVENT-0053`

`TARGETED INDEPENDENT RETEST OF F-01…F-10 ON CORRECTED S0 HEAD → PRESERVE/FREEZE S0 REMEDIATION EVIDENCE → MERGE` — `REMEDIATION_SESSION_S0` (`REMEDIATION-SESSION-S0-CONTRACT-FREEZE-001`, corrected substantive `8756a94824ba`) is `CORRECTED_PENDING_TARGETED_INDEPENDENT_RETEST`. `INDEPENDENT-ARCHITECTURE-RETEST-S0-001` returned `PASS_WITH_FINDINGS` (F-01…F-10); `REMEDIATION-SESSION-S0-CORRECTION-001` rewrote the unmerged two-commit delivery in place: F-01 Human-ratified (`ANOX-DECISION-S0-F01-RATIFICATION-001` — one-time, change-specific; S1 must not modify `tools/audit/validate_security_audit_evidence_preservation.py` before integration), F-02…F-10 fixed pending targeted retest. The next task is the targeted independent retest of the corrected S0 head (implementer ≠ retester, FCP-7), followed by the S0 evidence-preservation step (audit-evidence registry, MSC stage records `IMPLEMENTED`/`AUTOMATED_TESTED` → `INDEPENDENTLY_RETESTED`/`EVIDENCE_PRESERVED` as earned, evidence-preservation validator pin) and the human merge. In parallel, `REMEDIATION_SESSION_S1` (build provenance) remains authorized and may run on its own task with disjoint file ownership.

Preserved sequencing: … → HUMAN PRE-REMEDIATION DECISIONS AND AUTHORIZATION (**executed + preserved**) → `REMEDIATION_SESSION_S0` (**corrected — pending targeted independent retest of F-01…F-10**) ∥ `REMEDIATION_SESSION_S1` (authorized, not executed) → S0/S1 independent retests + evidence preservation → `S2 ∥ S3` → `S4` → native retest acceptance (037) → Pre-B004 Definition of Done → B004 (S5 requirements) → B006 → B008/B009 → B013 → RC → physical campaign → legacy/architecture revalidation → fresh full-system re-audit → operational acceptance → human final gate.

Do not start B-004/B-005, product code changes, the physical campaign (`PHYSICAL_P1..P17`), S2/S3/S4 or any other remediation session without a new authorized task and the required predecessor gates.
