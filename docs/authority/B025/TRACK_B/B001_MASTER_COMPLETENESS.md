# B-001 — Master Completeness
**Status:** DEFINED

Purpose: ensure the MAIN is an engineering-complete authority rather than a high-level summary. Required domains: product, identity/account/license, device auth, E2EE/key distribution, verification, messaging/sync, local DB/storage, contacts, push, attachments, backend/API/DB/RLS, lifecycle, privacy/retention, abuse, production infrastructure, supply chain, signing/updates, operations, UX, security tests, independent audit and release DoD.

Historical material must remain distinguishable from current authority. Missing history must be labeled missing, never reconstructed as fact. No security-critical implementation may start if its required V1 freeze is absent.
